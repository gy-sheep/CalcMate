# Q0004: 카드 배경에는 이미지를 사용하고 싶어. 요구하는 대로 처리가 가능한지 체크해줘

**날짜**: 2026-02-27
**원본**: [PROPMTS.md](../PROPMTS.md#q0004)

## 질문

1. 카드도 확장되는 느낌은 동일하지만 확장되며 fadeout 된다
2. 그래서 이후 전환된 화면에는 해당 이미지가 배경으로 보이지 않는다

## 답변

### 결론: 가능하다. `flightShuttleBuilder`로 구현한다.

---

### 동작 원리

Hero 위젯의 `flightShuttleBuilder` 파라미터를 사용하면 **비행(전환) 중에 보여줄 위젯을 직접 커스터마이징**할 수 있다.

1. **확장** — Hero가 카드 크기 → 전체 화면 크기로 보간 (기존 동작 유지)
2. **fadeout** — `flightShuttleBuilder` 안에서 `FadeTransition`으로 이미지를 사라지게 함
3. **도착 화면에 이미지 없음** — 도착 Hero의 `child`를 빈 `Container()`로 설정

```
[카드]            [전환 중]                    [상세 화면]
┌──────────┐     ┌────────────────┐           ┌─────────────┐
│  🌄 이미지│ →→  │ 🌄 (확장+fadeout)│    →→   │             │
└──────────┘     └────────────────┘           │  (이미지 없음)│
  Hero child        flightShuttleBuilder        │             │
  (이미지)           FadeTransition               └─────────────┘
                    opacity 1.0 → 0.0             Hero child
                                                  (빈 Container)
```

---

### 구현 코드

**`calc_mode_card.dart` — 카드 배경 Hero:**

```dart
Hero(
  tag: 'calc_bg_$title',
  flightShuttleBuilder: (_, animation, __, ___, ____) {
    return FadeTransition(
      opacity: Tween<double>(begin: 1.0, end: 0.0).animate(animation),
      child: Image.asset(imagePath, fit: BoxFit.cover),
    );
  },
  child: Image.asset(imagePath, fit: BoxFit.cover),
),
```

**`basic_calculator_screen.dart` — 도착 Hero를 빈 Container로:**

```dart
// Hero 도착지: 투명 (이미지가 fadeout 후 착지)
Positioned.fill(
  child: Hero(
    tag: 'calc_bg_$title',
    child: Container(),
  ),
),
// 실제 화면 배경은 Hero와 별도로 구성
Positioned.fill(
  child: Container(color: color),
),
```

---

### 주의사항

- `CalcModeCard`의 `color` 파라미터를 `imagePath`(String)로 교체한다
- 도착 Hero(`Container()`)가 `Positioned.fill` 안에 있어야 크기가 전체 화면 기준으로 잡힌다
- 상세 화면 배경은 Hero와 별도 레이어로 구성해야 한다 (Hero child가 투명하므로)
- `Image.asset`에 `errorBuilder`를 추가해 이미지 파일이 없을 때 폴백 처리를 권장한다
