# {기능명} 구현 명세

> **브랜치**: `{브랜치명}`
> **목적**: {한 줄 목적 설명}

---

## 프로젝트 구조 및 작업 범위

아래 구조에서 `[신규]`는 새로 생성되는 파일, `[수정]`은 기존 파일 변경을 나타낸다.

```
lib/
├── core/
│   └── ...
├── domain/
│   └── ...                   [신규/수정]
├── data/
│   └── ...                   [신규/수정]
└── presentation/
    └── ...                   [신규/수정]
```

**데이터 흐름**

```
{데이터 흐름을 ASCII 다이어그램으로 표현}
```

---

## 배경 및 문제

{현재 구조의 문제점 또는 이 기능이 필요한 이유}

| 문제 | 영향 |
|------|------|
| ... | ... |

---

## 목표

- {리팩터링/구현 후 달성 상태를 bullet로 나열}

---

## 구현 범위

### 1. `{파일 경로}` — {역할}

{설명}

```dart
// 핵심 코드 스케치
```

**설계 결정**

- `{필드/메서드}`: {이 결정을 내린 이유}

---

### 2. `{파일 경로}` — {역할}

{위와 동일한 패턴 반복}

---

## 파일 변경 목록

| 파일 | 작업 |
|------|------|
| `경로/파일명.dart` | 신규 생성 |
| `경로/파일명.dart` | 수정 |
| `경로/파일명.dart` | 자동 생성 (build_runner) |

---

## 구현 순서

```
1. {첫 번째 작업}
   └─ {세부 단계}
2. {두 번째 작업}
3. ...
```

---

## 범위 외 (이번 작업 제외)

- {이번에 하지 않는 것} — {이유 또는 예정 시점}
