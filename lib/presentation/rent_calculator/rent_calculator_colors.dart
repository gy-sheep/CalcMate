import 'package:flutter/material.dart';

// ── Background ────────────────────────────────────────────────
const kRentBgTop = Color(0xFFF5F0E8); // aged newsprint
const kRentBgBottom = Color(0xFFEDE7DD); // warm newsprint

// ── Accent ────────────────────────────────────────────────────
const kRentAccent = Color(0xFF1A1A1A); // ink black
const kRentAccentSoft = Color(0x121A1A1A);
const kRentAccentBorder = Color(0x301A1A1A);

// ── Sepia (포인트) ───────────────────────────────────────────
const kRentGold = Color(0xFF8B6914); // dark sepia gold
const kRentGoldSoft = Color(0x158B6914);

// ── Text ──────────────────────────────────────────────────────
const kRentTextPrimary = Color(0xFF1A1A1A); // headline ink
const kRentTextSecondary = Color(0xFF4A4540); // body ink
const kRentTextTertiary = Color(0xFF8A8279); // faded print

// ── Cards ─────────────────────────────────────────────────────
const kRentCardBg = Color(0xFFFCF9F4); // fresh newsprint
const kRentCardBorder = Color(0xFFD6CEBC); // aged edge
const kRentResultBg = Color(0xFFF0EBE0); // column highlight
const kRentResultBorder = Color(0xFFCCC4B4); // ruled line

// ── UI Elements ───────────────────────────────────────────────
const kRentDivider = Color(0xFF1A1A1A); // thin rule
const kRentWarning = Color(0xFFA03020); // editorial red
const kRentSuccess = Color(0xFF2A5A3A); // financial green
const kRentSwapBg = Color(0x0A1A1A1A);

// ── Tab ───────────────────────────────────────────────────────
const kRentTabDivider = Color(0xFF9A9288);
